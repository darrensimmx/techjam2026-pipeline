"""Test package.

This file exists so `python -m unittest discover` finds these tests. Without it
`tests/` is not an importable package, discovery walks straight past it, and the
run reports `Ran 0 tests ... OK` -- green output that means "found nothing",
not "everything passed".
"""
