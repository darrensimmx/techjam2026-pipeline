# Phase 1 has no third-party dependencies -- standard library only
# (json, re, sqlite3, argparse, subprocess, unittest). Kept this way
# deliberately: fewer dependencies is less that can fail or need network
# access under the organizer's final-scoring restrictions.
